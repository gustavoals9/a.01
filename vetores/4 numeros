#include <stdio.h>
int main(){
    int valores[4];
    valores[0]=4;
    valores[1]=7;
    valores[2]=3;
    valores[3]=1;

    printf("Os valores armazenados sao: ");
    for(int i=0; i<4; i++){
        printf("%d", valores[i]);
    }
    printf("\nFim de programa!");
    return 0;
}
