#include <stdio.h>
int main(){

    for(int contador=1; contador<=10; contador++){
        if(contador == 5){
            continue;
        }else{
            printf("O valor da contagem e %d\n", contador);
        }
    }
    return 0;
}
